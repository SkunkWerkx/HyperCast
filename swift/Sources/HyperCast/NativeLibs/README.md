Native builds land here as NativeLibs/{rid}/{lib} — staged locally from the in-repo
cargo build for development, populated for every platform by CI before packaging. The
binaries themselves are gitignored; this README holds the directory.
