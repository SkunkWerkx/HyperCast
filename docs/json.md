# JSON ingestion — parked design

Follow-up to a real design conversation (2026-08-31), banked here so it doesn't get lost.
Status: **parked** — nothing here is committed work; the round-three tabular layer
([roadmap](roadmap.md)) comes first and this rides the same contract. Everything below is
the reasoning, not just the conclusions, so future-us can disagree with specifics without